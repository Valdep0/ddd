<Page x:Class="MasterPolApp.Views.EmployeesWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Сотрудники" Background="#FFFFFF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Сотрудники компании" FontSize="22" FontWeight="SemiBold" Foreground="#333333"/>

        <Border Grid.Row="1" Background="#F4E8D3" CornerRadius="10" Padding="15" Margin="0,15,0,15">
            <StackPanel>
                <TextBlock Text="👥 Штат сотрудников" FontSize="14" FontWeight="SemiBold" Foreground="#333333"/>
                <TextBlock x:Name="txtStats" FontSize="13" Foreground="#666666" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgEmployees" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F4E8D3" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ФИО" Width="200" Binding="{Binding full_name}"/>
                <DataGridTextColumn Header="Должность" Width="150" Binding="{Binding position}"/>
                <DataGridTextColumn Header="Телефон" Width="120" Binding="{Binding phone}"/>
                <DataGridTextColumn Header="Дата приема" Width="120" Binding="{Binding hire_date, StringFormat='dd.MM.yyyy'}"/>
                <DataGridTextColumn Header="Пользователь" Width="120" Binding="{Binding username}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>