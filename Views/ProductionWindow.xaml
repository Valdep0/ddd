<Page x:Class="MasterPolApp.Views.ProductionWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Производство" Background="#FFFFFF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Производственные заказы" FontSize="22" FontWeight="SemiBold" Foreground="#333333"/>

        <Border Grid.Row="1" Background="#F4E8D3" CornerRadius="10" Padding="15" Margin="0,15,0,15">
            <StackPanel>
                <TextBlock Text="🏭 Активные производственные задания" FontSize="14" FontWeight="SemiBold" Foreground="#333333"/>
                <TextBlock x:Name="txtStats" FontSize="13" Foreground="#666666" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgProduction" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F4E8D3" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Продукция" Width="200" Binding="{Binding product_name}"/>
                <DataGridTextColumn Header="Мастер" Width="150" Binding="{Binding master_name}"/>
                <DataGridTextColumn Header="Материал" Width="150" Binding="{Binding material_name}"/>
                <DataGridTextColumn Header="Дата начала" Width="120" Binding="{Binding start_date, StringFormat='dd.MM.yyyy'}"/>
                <DataGridTextColumn Header="Количество" Width="100" Binding="{Binding quantity_produced}"/>
                <DataGridTextColumn Header="Статус" Width="120" Binding="{Binding status}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>