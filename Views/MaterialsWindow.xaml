<Page x:Class="MasterPolApp.Views.MaterialsWindow"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Склад" Background="#FFFFFF">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Склад и материалы" FontSize="22" FontWeight="SemiBold" Foreground="#333333"/>

        <Border Grid.Row="1" Background="#F4E8D3" CornerRadius="10" Padding="15" Margin="0,15,0,15">
            <StackPanel>
                <TextBlock Text="📊 Статистика склада" FontSize="14" FontWeight="SemiBold" Foreground="#333333"/>
                <TextBlock x:Name="txtStats" FontSize="13" Foreground="#666666" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgMaterials" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  AlternatingRowBackground="#F4E8D3" FontSize="13">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Width="200" Binding="{Binding material_name}"/>
                <DataGridTextColumn Header="Тип" Width="150" Binding="{Binding material_type}"/>
                <DataGridTextColumn Header="Остаток, ед" Width="120" Binding="{Binding stock_quantity}"/>
                <DataGridTextColumn Header="Брак, %" Width="100" Binding="{Binding defect_rate}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>